{
  "name": "marko-tsrx-repo",
  "private": true,
  "description": "Marko TSRX compiler (@marko/tsrx) and Vite plugin (@marko/vite-plugin-tsrx)",
  "license": "MIT",
  "author": "LuLaValva",
  "type": "module",
  "workspaces": [
    "./packages/*",
    "./examples/*"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "format": "prettier --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "publish": "npm run build && npm publish --workspace packages/tsrx && npm publish --workspace packages/vite-plugin-tsrx"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "playwright-core": "^1.59.1",
    "prettier": "^3.8.3",
    "prettier-plugin-packagejson": "^3.0.2",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^4.0.16"
  }
}
