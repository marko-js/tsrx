export default component App() {
  const ready = false;
  const maybe = false;

  if (ready) {
    <p class="yes" />
  } else if (maybe) {
    <p class="maybe" />
  } else {
    <p class="no" />
  }
}
